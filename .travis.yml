language: python
sudo: false
python:
    - 2.7
install:
    - pip install setuptools zc.buildout
    - pip install coveralls coverage
    - buildout bootstrap
    - buildout
script:
    - coverage run bin/test -v1
after_success:
    - coverage combine
    - coveralls
cache:
  pip: true
  directories:
    - eggs/
