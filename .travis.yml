language: python
python: 2.7
sudo: false
cache:
  pip: true
  directories:
    - $HOME/buildout-cache
before_install:
  - mkdir -p $HOME/buildout-cache/{eggs,downloads}
  - pip install setuptools zc.buildout
install:
  - buildout bootstrap
  - buildout
script:
  - bin/test
