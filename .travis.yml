language: python
sudo: false
python:
    - 2.7
env:
    - EXTRA=
    - EXTRA=-zserver
install:
    - pip install tox
script:
    - tox -e py${TRAVIS_PYTHON_VERSION//.}$EXTRA
after_success:
    - coverage combine
    - coveralls
cache:
  pip: true
  directories:
    - eggs/
